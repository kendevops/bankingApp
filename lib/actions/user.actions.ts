"use server";

export const signIn = async () => {
    try {
      // Mutation / Database changes / Make fetch
  } catch (error) {
    console.error("Error", error);
  }
};
export const signUp = async ({userData: SignUpParams}) => {
    try {
      // Mutation / Database changes / Make fetch
  } catch (error) {
    console.error("Error", error);
  }
};
